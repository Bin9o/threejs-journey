"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from) {
    for (var i = 0, il = from.length, j = to.length; i < il; i++, j++)
        to[j] = from[i];
    return to;
};
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Sorts an array of objects, ordered by properties and orders.
 * @param arr
 * @param props
 * @param orders
 * @returns
 */
var orderBy = function (arr, props, orders) {
    return __spreadArray([], __read(arr)).sort(function (a, b) {
        return props.reduce(function (acc, prop, i) {
            if (acc === 0) {
                var _a = __read(orders && orders[i] === "desc"
                    ? [b[prop], a[prop]]
                    : [a[prop], b[prop]], 2), p1 = _a[0], p2 = _a[1];
                acc = p1 > p2 ? 1 : p1 < p2 ? -1 : 0;
            }
            return acc;
        }, 0);
    });
};
exports.default = orderBy;
