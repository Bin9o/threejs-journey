"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * Clamps `num` within the inclusive range specified by the boundary values `lower` and `upper`.
 * @param num
 * @param lower
 * @param upper
 * @returns
 */
var clamp = function (num, lower, upper) {
    return Math.max(Math.min(Number(num), Math.max(lower, upper)), Math.min(lower, upper));
};
exports.default = clamp;
